const styles = (theme) => ({
    table: {
        minWidth: 650,
    },
    label: {
        display:'flex',
        justifyContent: 'center',
        alignItems: 'center',
        margin: '40px'
    }
});
export default styles;